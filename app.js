const search=document.querySelector('#menu-search');if(search){search.addEventListener('input',()=>{let count=0;const q=search.value.toLocaleLowerCase('lt').trim();document.querySelectorAll('.menu details').forEach(group=>{let n=0;group.querySelectorAll('article').forEach(item=>{item.hidden=!item.textContent.toLocaleLowerCase('lt').includes(q);if(!item.hidden)n++});group.hidden=n===0;if(q&&n)group.open=true;count+=n});document.querySelector('#empty').hidden=count>0})}if(!matchMedia('(prefers-reduced-motion: reduce)').matches){document.querySelector('.hero-copy')?.animate([{opacity:0,transform:'translateY(24px)'},{opacity:1,transform:'translateY(0)'}],{duration:850,easing:'ease-out'});const observer=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){e.target.animate([{opacity:.25,transform:'translateY(22px)'},{opacity:1,transform:'translateY(0)'}],{duration:650,easing:'ease-out'});observer.unobserve(e.target)}})},{threshold:.1});document.querySelectorAll('.intro,.feature,.menu-top').forEach(e=>observer.observe(e))}